# 测试改进计划（中文镜像）

> 快照日期：2026年3月24日。本文件用于记录测试工程的长期背景与策略；日常可执行 backlog 以 `docs/agents/test_todo_list.md` 为准。镜像约束：每次变更需同步英文版 `docs/agents/test_improvement_plan.md`。

## 当前基线

- 最新已验证全量运行命令：`npm run test:coverage:report`（随后执行 `npm run test:coverage:assert-full`）
- 当前全量运行规模：309 suites / 3687 tests
- 当前 Jest 全量覆盖率：
  - Statements: 100.00% (21869/21869)
  - Branches: 100.00% (12560/12560)
  - Functions: 100.00% (4748/4748)
  - Lines: 100.00% (20961/20961)
- 当前逐文件库存：追踪 334 个源码文件，已全满 334 个，仍有缺口 0 个。
- 当前 branch 分桶：`<50 = 0`、`50-70 = 0`、`70-90 = 0`、`90-<100 = 0`。
- `jest.config.cjs` 当前全局门槛：50%（branches/functions/lines/statements）。
- 当前战略任务：**全仓收口仍保持完成状态。工作重点是维护态：保持所有触达或新增的 `src/**`文件继续维持`100/100/100/100`，一旦未来有回归，就立刻重新开启有序队列。\*\*

## 原则

- 长期目标：把 `src/**` 推进到 100% 的有效覆盖。
- 覆盖率报告里的队列是执行顺序的事实来源；不要再按主观“哪个收益更高”改顺序。
- 当队列非空时，一次处理一个队列文件，尽量把它推进到 `100/100/100/100` 后再移动。
- 当队列为空时，进入维护态，所有触达或新增的 `src/**` 文件都必须继续保持 `100/100/100/100`。
- 允许的策略例外很少：相邻文件共享同一套 mock/fixture/test harness 时可成批推进；当前文件或紧邻文件被共享测试基础设施问题卡住时，可先修 blocker。
- 如果当前队列分支被证明不可达或业务上无效，优先在不改变行为的前提下删掉死分支，而不是为了抬覆盖率去编造测试。
- 复用 `tests/helpers/**` 与 `tests/mocks/**`，避免临时造 mock。
- 测试改动保持小范围、可重复、可确定。
- 每次修改后必须执行 `npm run lint`。
- 现阶段仍不要上调覆盖率阈值；先证明全仓满覆盖能够在维护态下稳定保持。

## 集成测试的战略目标

- 既然全仓覆盖率已经收口，下一阶段最重要的质量收益应来自“更强的工作流信心”，而不是逼着 `tests/integration` 单独去追 `100%` 代码覆盖率。
- 集成测试完成度统一按 `docs/agents/ai-testing-guide.md` 中的 100 分量表定义：
  - 功能工作流覆盖，
  - 路由/角色/query 矩阵覆盖，
  - 失败与回退行为，
  - 导航与接近浏览器边界的副作用，
  - 工程质量。
- 默认扩展层：integration tests。
- 默认 E2E 范围：只保留很薄的一层冒烟，专门用于 integration 无法可信模拟的浏览器真实行为。
- 高风险工作流的发布就绪目标：`>=85/100`。
- 对现有 brownfield 工作流，按阶段逐步抬升，不要一次性重写所有现有 integration suite。

## 分阶段集成测试路线图

1. 路由/数据源矩阵阶段
   - 退出标准：所有由 pathname 驱动的共享数据页面，都在 integration 中覆盖 `/mydata` 以及至少一个非 `/mydata` 前缀。
   - 升级规则：如果页面在不同数据源下确实存在行为差异，就扩展为完整的 `/mydata` + `/tgdata` + `/codata` + `/tedata` 四路矩阵。
2. 权限和角色阶段
   - 退出标准：`Teams`、`ManageSystem`、`Review` 都覆盖 allow、restricted、failure 三类状态。
3. URL/query 与导航阶段
   - 退出标准：登录重定向、深链打开状态、跨页导航契约都在 integration 中有断言。
4. 失败与回退阶段
   - 退出标准：列表加载失败、create/update 失败、delete 失败或取消、以及有意义的空状态，都有用户可见处理路径。
5. 可选的浏览器真实冒烟阶段
   - 退出标准：只有极少量 E2E 冒烟存在，并且只聚焦重定向链、上传/预览、reload 行为、整页导航这些浏览器真实场景。

## 维护态待办

- [ ] 在维护态下保持所有触达或新增的 `src/**` 文件继续维持 `100/100/100/100`。
- [ ] 把任何新出现的未覆盖分支都视为需要立即消掉的回归，而不是新的长期 backlog。
- [ ] 在有意义的测试工程变更后，重新运行 `npm run test:coverage:report`，确认队列仍为空。
- [ ] 保持本地 push 门禁与文档、脚本一致（`npm run prepush:gate` 必须继续是唯一权威的本地强制命令）。
- [ ] 当人要求扩大工作流层面的测试信心时，按 `docs/agents/test_todo_list.md` 里的阶段化集成测试计划推进，而不是零散追加一次性场景。
- [ ] 未来如果投入 E2E，必须刻意保持很薄，只覆盖浏览器真实行为。

## 执行循环

1. 先运行 `npm run test:coverage:report`。
2. 如果报告显示没有剩余缺口，就保持维护态，并确保所有触达的 `src/**` 文件继续维持 `100/100/100/100`。
3. 如果报告显示有缺口，就从 `docs/agents/test_todo_list.md` 的有序清零队列里拿第一个文件。
4. 如果紧邻文件共享同一套 mock/fixture/test harness，就在同一套 setup 下顺手一起收口；否则坚持单文件推进。
5. 为未覆盖分支和缺失的编排路径补测试；如果剩余分支被证明是死分支，就在不改变行为的前提下删掉它，然后继续队列。
6. 用 `npm run test:ci -- <pattern> --runInBand --testTimeout=... --no-coverage` 跑聚焦套件。
7. 运行 `npm run lint`。
8. 再跑全量覆盖率（`npm run test:coverage`）并复查 `npm run test:coverage:report`；只有在队列非空且需要完整逐文件状态时，才展开 `node scripts/test-coverage-report.js --full`。
9. 在 `docs/agents/test_todo_list.md` 记录增量；只有当战略执行规则、库存摘要或队列例外策略变化时，再同步更新本文件。

## 单项完成定义

- 测试稳定、可重复通过。
- 无无说明 `skip`。
- `npm run lint` 通过。
- 覆盖率增量可度量，并记录在 `docs/agents/test_todo_list.md`。
- 当前队列文件已完成收口，或全仓仍保持维护态且不存在剩余缺口。
- 若仍有未覆盖分支，必须有明确记录说明它不可达或业务上无效。
- 只有当长期执行规则、库存摘要或队列例外策略变化时，再更新本文件。
