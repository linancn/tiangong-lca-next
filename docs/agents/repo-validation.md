---
title: next Repo Validation Guide
docType: guide
scope: repo
status: active
authoritative: false
owner: next
language: en
whenToUse:
  - when a change is ready for local proof
  - when deciding the minimum evidence required for a PR
  - when writing validation notes for this repo
whenToUpdate:
  - when canonical commands or quality gates change
  - when change categories require different proof
  - when coverage or deploy policy changes
checkPaths:
  - docs/agents/repo-validation.md
  - .docpact/config.yaml
  - package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - .oxlintrc.json
  - .prettierignore
  - .prettierrc.js
  - .ncurc.json
  - jsconfig.json
  - tsconfig*.json
  - playwright.config.ts
  - config/docs-capture/**
  - scripts/e2e/**
  - docker/e2e/**
  - tests/e2e/i18n/**
  - jest.config.cjs
  - Dockerfile.app
  - .husky/pre-commit
  - scripts/jest-sequencer.cjs
  - scripts/oxlint-plugin-tiangong.mjs
  - .husky/pre-push
  - scripts/prepush-gate-receipt.cjs
  - scripts/release/**
  - scripts/test-runner.cjs
  - scripts/typescript-native-parser.*
  - scripts/i18n/locale-delivery.mjs
  - .github/workflows/**
lastReviewedAt: 2026-08-29
lastReviewedCommit: f61a8699264b10a42c483f85333d42540a394327
lastReviewedNote: 'Reviewed for Next Issue #955: focused strict-act Processes proof plus lint and the final full gate fit the existing validation matrix; no gate rule changes are needed.'
related:
  - ../AGENTS.md
  - ../.docpact/config.yaml
  - ./repo-architecture.md
---

## Validation Order

1. identify the change type
2. run the minimum proof for that change type
3. add stronger proof only when the risk actually increases
4. record exact commands and environments in the PR

## Default Baseline

Unless the change is doc-only repo-maintenance work, the minimum local baseline is:

```bash
pnpm lint
pnpm test
pnpm build
```

The authoritative protected-branch gate is:

```bash
pnpm docpact:gate
```

```bash
pnpm prepush:gate
```

## Proof Matrix

| Change type | Minimum local proof | Stronger proof when risk is higher | Notes |
| --- | --- | --- | --- |
| routes, pages, app runtime, shared UI | `pnpm lint`; focused `pnpm test:ci <jest-args>`; `pnpm build` | `pnpm prepush:gate` | shared UX changes often affect multiple entrypoints; Process LCIA query/evidence state changes include `tests/unit/pages/Processes/Components/processLciaResultsPanel.test.tsx` |
| services or env selection | `pnpm lint`; focused `pnpm test:ci <jest-args>`; `pnpm build` | `pnpm prepush:gate` | companion proof may live in another repo if schema or Edge runtime changed |
| Process/Flow AI suggestion transport | `pnpm test:ci --runInBand tests/unit/services/general/aiSuggestion.test.ts`; `pnpm lint`; `pnpm build` | final `pnpm prepush:gate` through `push:checked`; smoke against matching non-production Database, Edge, and generic `ai-worker` revisions when deployed | Assert authenticated enqueue, requester-scoped polling, bounded progressive intervals, timeout/abort, all terminal failures, malformed projections, and exact `ai.tidas_suggestion.result.v1` complete/partial decoding. Keep diff generation and selective acceptance in the existing component; do not expose worker payloads, leases, diagnostics, or provider details. |
| Process or Flow TIDAS scalar storage normalization | `pnpm lint`; focused general scalar-normalizer, Process/Flow serializer, and Process/Flow API suites; `pnpm build` | final `pnpm prepush:gate` through `push:checked` | Assert Process years persist as integers in `1000..9999`, affected percentage values persist as TIDAS-compatible strings with at most three fractional digits, zero is retained, and create/update/create-version reject non-canonical non-empty values before persistence. |
| TIDAS package export task identity or recovery | `pnpm test:ci --runInBand tests/unit/services/tidasPackage/taskCenter.test.ts`; `pnpm lint`; `pnpm build` | final `pnpm prepush:gate`; pair with Database package-cache pgTAP proof when enqueue semantics changed | Assert repeated queue responses, persisted aliases, poll completion/failure, and Worker refreshes sharing `workerJobId` or `jobId` produce one visible task; retain the earliest local presentation identity, use backend timestamps/state, and never revive an alias after canonical reconciliation. |
| Process keyword search service or LCA picker scope | focused `tests/unit/services/processes/api.test.ts` plus the Process full-text data-workflow unit; `pnpm lint`; `pnpm build` | smoke against the exact non-production Database revision that exposes `search_processes`; cover public, personal, and `public_plus_owner_draft` scopes, including actor-owned state-zero rows with non-null team/review metadata | Assert explicit `query_terms`, no direct app-side ILIKE field filter, and `owner_draft_only=true` only for the personal branch of the calculation scope. Database owns latest-version, actor/state eligibility, workflow metadata, and `search_text` index semantics. |
| LCA solve, result-query, contribution-path, or task-recovery scope contract | focused LCA API, task-center service/component, Process analysis, solve toolbar, impact compare/hotspot, and LCIA result panel suites; `pnpm lint`; `pnpm build` | smoke the authenticated Process analysis paths against matching non-production Edge and Database revisions | Assert every new request uses `full_library` by default or explicit `data_product`; deployment/cache labels never cross the API boundary; persisted historical task requests are normalized before recovery resubmission. |
| Contact, FlowProperty, Source, or UnitGroup Hybrid Search service/page/picker | `pnpm lint`; focused shared-helper, four service, four main-page, four picker, locale-contract, and production-request-guard Jest suites; `pnpm build` | final `pnpm prepush:gate` through `push:checked`; smoke the exact non-production frontend against matching Database and Edge revisions for `tg`/`co`/`my`/`te` | Keep UUID and empty-keyword paths unchanged; verify state/team forwarding and Team Data without a selected team; auth, transport, malformed-response, and mapping failures must remain `success: false` rather than a successful empty result. New read-only Edge paths require an exact production-request-guard allowlist entry. |
| persisted Calculation Bundle or public release readback | `pnpm lint`; focused `lcaReleases`, `CalculationBundlePanel`, `LcaReleaseReadPanel`, Data Processing, Process view, and locale tests; `pnpm i18n:audit`; `pnpm build` | `pnpm prepush:gate`; paired Database RPC, Edge projection, Worker bundle, and deterministic package proof; live smoke only when a deployed environment and user credentials are available | private bundle reads use the user session; the normal download surface contains LCIA XLSX/CSV, LCI Parquet/CSV, manifest, and one audit ZIP without enumerating canonical shards; every download verifies stored byte size and SHA-256 before Blob save; public release reads expose sanitized projections and short-lived artifact downloads only |
| Data Product ResultSet, closure/task, and artifact presentation | `pnpm lint`; focused ResultSet decoder, Data Processing, global Task Center, closure artifact/command decoder, production-request guard, and locale tests; `pnpm i18n:audit`; `pnpm build` | `pnpm prepush:gate`; smoke create/select/continue, failed/blocked closure checks, package preview/publication, and ready, preparing, expired, and unavailable artifacts against matching non-production Database/Edge/Worker revisions | assert ResultSet create/list/select, stable `resultSetId` URLs, server-projected Task Center continuation, derived three-stage status/next action, and filtering of checks/packages/publications to the selected ResultSet; execution failure must not render as an empty closure-issue result; preserve the curated paginated issue list, keep XLSX and machine-manifest actions separate, expose semantic metadata only, navigate directly to signed URLs without buffering bytes, and map owner-visible 410 expiry to localized rerun guidance |
| scope-closure provider-owned Next qualification adapter | `pnpm test:qualification:scope-closure:unit`; exact clean-commit `scripts/qualification/scope-closure-next-qualification.mjs --output <outside-repo-json> --run-id <uuid>` with loopback `QUALIFICATION_SUPABASE_URL` and the explicit isolated-target confirmation | run the adapter twice with the same run ID and compare canonical bytes; validate each record with the exact Worker provider aggregator revision | The adapter creates a temporary detached worktree, runs the actual Umi page in Chromium, blocks unexpected origins, covers anonymous/standard/admin/owner/manager routing plus preparing/available/expired/unavailable/download/410-localization states, and emits only the four Next-owned `consumers` leaves. Output must contain no credentials, tokens, cookies, URLs, locators, or payload data. |
| review submission admission or Review Admin quality diagnostic | `pnpm lint`; focused `pnpm test:ci tests/unit/services/reviews/api.test.ts tests/unit/utils/review.test.ts tests/unit/pages/Processes/Components/edit.test.tsx tests/unit/pages/Flows/Components/edit.test.tsx tests/unit/pages/Sources/SourceEdit.test.tsx tests/unit/pages/Contacts/ContactEdit.test.tsx tests/unit/pages/Review/index.test.tsx tests/unit/pages/Review/Components/ReviewQualityDiagnostic.test.tsx --runInBand --testTimeout=30000`; `pnpm i18n:audit`; `pnpm build` | final `pnpm prepush:gate`; smoke stable submit plus diagnostic start/read against matching non-production Database, Edge, and Worker revisions | Process, Flow, Source, and Contact recursively traverse their existing reference chains before calling the stable dataset command; Process also validates its saved current record. Assert missing or inaccessible references, failed rule verification, and stale published referenced versions block submission, while another active version neither blocks submission nor produces a Check Data warning; no Worker job is inspected and no Gate/checksum/revision field is sent. The diagnostic is Review Admin-only, starts only after a click, uses server-selected pending-review scope, combines completeness and numerical-stability sections, and leaves assignment, approval, and rejection available for every outcome and failure state. Legacy Gate/task-center readers are compatibility-only and receive no new submit traffic. |
| unified seven-type Root/Reference Review UI | `pnpm tsc`; focused Oxlint for changed pages/services/locales; `pnpm exec jest tests/unit/services/reviews/api.test.ts tests/unit/components/DataNotification.test.tsx tests/unit/pages/Review/Components/AssignmentReview.test.tsx tests/unit/pages/Review/Components/SelectReviewer.test.tsx tests/unit/pages/Review/Components/ReviewQualityDiagnostic.test.tsx tests/unit/utils/review.test.ts tests/unit/utils/review.validation.test.ts --runInBand --no-watchman`; `pnpm build` | final `pnpm prepush:gate`; smoke all seven edit-page submit actions plus Admin/Member queues against matching Database and Edge revisions | All seven types submit without completeness or numerical-stability Gate fields. Process alone applies its current-record SDK/exchange/quantitative-reference checks; Process, Flow, Source, and Contact apply recursive reference-access, rule-verification, and referenced-version checks. Exact under-review snapshot References may be resubmitted, exact Root snapshots remain blocked, and other versions may be reviewed in parallel without a validation issue; simple approval has no opinion and rejection requires a reason; top-level pages flatten matching Roots and References with backend-owned count/pagination; display mode and exact data type are forwarded to Database, combine by intersection, reset incompatible selection/page state, and default top-level pagination to 50; only Process/Lifecycle Model Roots expand and their relationship child table remains unfiltered; top-level References select and act independently; expandable Root selection loads current-tab children, deduplicates shared References, preserves manual selection, and fails closed while loading fails; other Root types never request children; every readable Root/Reference opens the correct existing seven-type view drawer; Reviewer rejections count as completed opinions; dataset detail pages do not gain rejection displays; and owner notifications route to the correct edit page. |
| reviewed LCIA bundle under `public/lciamethods/**` or its validator | `pnpm lcia-cache:verify`; `pnpm lint`; focused LCIA cache/evidence tests; `pnpm build` | `pnpm prepush:gate` | verification must run before any web or Electron publication |
| governed classification/location sources, overlays, manifest, generator, or runtime assets | `pnpm reference-data:check`; focused reference-resource/resolver/cache tests; `pnpm lint`; `pnpm build` | `pnpm reference-data:production:check`; `pnpm i18n:locale:all:production:check`; `pnpm prepush:gate` | generation must be deterministic; every native overlay and independent project review must have exact identity coverage; provenance and usage-term blockers remain fail closed |
| other static bundles under `public/**` | `pnpm lint`; `pnpm build` | focused tests near the consuming feature | check both the asset and its readers |
| sync helpers under `docker/**` | `bash -n docker/pull-edge-functions.sh`; verify `--help` is non-mutating; `pnpm lint`; `pnpm build` | run the helper with one reviewed full commit SHA, verify the source receipt and stale-file deletion, then rerun the same command and require an idempotent tracked tree | do not hand-edit synced mirrors or follow a moving ref |
| TypeScript, Oxlint, Prettier, or repository source-analysis toolchain | `pnpm test:ci tests/unit/config/toolchainCommandContract.test.ts tests/unit/scripts/typescriptNativeParser.test.ts --runInBand --no-coverage`; `pnpm lint`; `pnpm tsc:electron`; `pnpm build` | final `pnpm prepush:gate` through `push:checked`; rerun all i18n/source-analysis contract suites when the native parser adapter changes | TypeScript `7.0.2` is the only compiler/API track; no TS6 alias or `tsc6` is allowed. A whole-repository import scan permits `typescript/unstable/*` only in the native parser adapter and declaration. Oxlint owns unused/deprecated correctness, while `scripts/oxlint-plugin-tiangong.mjs` preserves the unsupported legacy `no-invalid-this` rule; Prettier does not organize imports. The qualified Umi build remains Webpack plus repo-owned typechecks; optional Mako `forkTSChecker` requires separate TS7 compatibility proof before enablement. |
| React 19, antd 6, ProComponents 3, Umi App/theme, or shared component API migration | `pnpm test:ci tests/unit/config/packageManagerContract.test.ts tests/unit/app.test.tsx tests/unit/contexts/AntdAppContext.test.tsx tests/unit/contexts/AntdThemeSync.test.tsx tests/unit/e2e/proComponentsSurfaceMatrixContract.test.ts --runInBand --no-coverage`; affected component/Form.List suites; `pnpm tsc`; `pnpm tsc:electron`; `pnpm build`; official `antd doctor` plus v6 source lint | final `pnpm prepush:gate`; credential-free hermetic browser qualification for the business PR; Electron release matrix | Keep exact React/DOM `19.2.8`, antd `6.6.1`, icons `6.3.2`, and ProComponents `3.1.14-6`. Umi's global ConfigProvider/App is the only provider generation, every feedback caller uses `App.useApp()` or the registered non-component dispatcher, and source/tests contain no antd 5 patch, split ProComponents import, CommonJS antd deep import, or member-style component API. Audit every real `Form.List` against antd 6 registered-field submission; keep every runtime Pro Components JSX instance uniquely mapped in the surface registry; use public semantic `classNames`/`styles` and project-owned browser locators for Modal, Drawer, Dropdown, Card, Divider, and table toolbar surfaces; prove main/nested option strips, responsive selector scrolling, forms, layouts, and overlays through the existing Playwright declarations; omit empty `waterMarkProps`; use Image preview `open`/`onOpenChange`; and retain the reviewed `virtual={false}` language selector exception because its bounded option set intentionally preserves complete semantic DOM. |
| package manager, dependency lock, install policy, CI, container dependency layer, or TIDAS SDK consumer | clean `pnpm install --frozen-lockfile`; `pnpm ignored-builds`; `pnpm test:ci tests/unit/config/packageManagerContract.test.ts tests/unit/config/toolchainCommandContract.test.ts tests/unit/config/installedTidasSdkContract.test.ts --runInBand --no-coverage`; `pnpm lint`; `pnpm tsc:electron`; `pnpm build` | focused release/pre-push/E2E identity suites; final `pnpm prepush:gate`; Electron platform matrix in the release workflow | Node stays exact `24.19.0`, `packageManager` and the pnpm engine stay exact `11.24.0`, TypeScript stays exact `7.0.2`, and `@tiangong-lca/tidas-sdk` stays exact `0.2.0`. Application and E2E Node images retain immutable multi-architecture digests, E2E identity binds its Node image reference, and every owned `pnpm/setup` use is fixed to the reviewed peeled executable v2.0.2 commit SHA rather than a tag object. The installed-package contract bypasses Jest's SDK mapper and proves all seven factories plus the normalized `validateEnhanced` failure envelope. Isolated linking remains the baseline. Only the reviewed `@babel/*` and `umi` public-hoist patterns are allowed; narrow workspace overrides collapse Umi's published React 18 / antd 4 / ProComponents 2 fallbacks to the exact native stack. Do not enable a hoisted linker, `shamefully-hoist`, or broad dependency override. Every lifecycle script decision is explicit, frozen installs must report no automatically ignored build, and `pnpm-lock.yaml` plus `pnpm-workspace.yaml` remain part of dependency-image, receipt, and qualification identity. |
| tests, coverage, or gate scripts | `pnpm docpact:gate`; `pnpm lint`; focused contract proof for the changed runner or script; exercise `TIANGONG_AGENT_MODE=1` when compact output changes | final `pnpm prepush:gate` through `push:checked` | do not precede the final gate with a duplicate full-suite or coverage run; slow-first scheduling must preserve discovery and the complete inventory; reusable receipt seeds must still copy an isolated repository and bare remote per test; every macOS Jest worker inherits `--no-concurrent-recompilation --no-maglev`, and the unit pool remains at 25% with a `512MB` idle-memory recycle boundary for the documented Node 24/V8 crash signature; agent mode writes full logs and structured results under `.local/test-logs/**`; coverage expectations remain strict |
| deterministic release/promotion orchestration under `scripts/release/**` | `pnpm test:release-workflow`; both command `--help` surfaces; JSON dry-run against exact remote identities; direct-ancestry, exact tree-identical two-parent promotion, and tree/lineage-drift fixtures; separate version-candidate and cumulative `main`-to-`dev` path fixtures; real Docpact fixed-point canary in an isolated `dev` clone; focused proof for release-proof and push-profile code; `pnpm docpact:gate`; `pnpm lint` | final ordinary implementation push through full `push:checked`; one real deterministic release canary using a tracked Issue and writable fork | stdout remains one schema-versioned JSON document; release-candidate and immutable-promotion branch profiles are restricted to exact generated branch/state/path identities and run only Docpact plus static preflight; the exact dev Release PR owns the non-browser release gate and an external proof bound to both bases, candidate tree/version, PR, workflow run/attempt, and artifact; main PR/push must reject missing or mismatched proof without a late full-gate fallback; promotion never alters the exact merged dev candidate |
| Playwright semantic localization E2E, release runner, route/view assertion contract, or evidence schema | `pnpm i18n:evidence:canonical:check --path <artifact>`; focused `pnpm e2e:dev --list` / affected browser project; focused runner unit tests; `pnpm e2e:env:doctor` when the pinned image is already installed; `pnpm lint` | exact clean-candidate `pnpm e2e:release`; add `--authenticated --allow-production-data --write-verified-evidence --users-env-file <0600-file>` only in an explicitly authorized local operator session | the evidence reporter writes Prettier-canonical JSON directly and resolves formatting from the repository-owned canonical path even when the destination is ignored or `/e2e-output`; no semantic E2E GitHub Actions event may receive production credentials or write production; authenticated proof is role-neutral globally, must close all 49 assertion IDs, and must finish with exact verified-row cleanup plus `created=cleaned`, `leaked=0` |
| source-bound docs capture profile, login/identity test IDs, runtime/readiness contract, or access-denied marker | focused `pnpm lint`; `pnpm build` when shipped markup changes | workspace v2 compiler tests plus one authorized end-to-end canary capture, privacy review, and paired `next-docs` asset validation | Next owns only facts that must evolve with the rendered commit. The workspace engine owns profile validation, plan/result/access schemas, credentials, read-only enforcement, dynamic origin, and Draft/evidence policy. A missing or incompatible profile is a blocker, never verified access denial. |
| locale bundles, language capabilities, message IDs, or localized runtime copy | `pnpm i18n:audit`; `pnpm i18n:platform:audit`; `pnpm i18n:hardcoding:audit`; `pnpm reference-data:check`; generate all registry summaries once with `pnpm i18n:locale:artifacts:write`; prove a second generation is a no-op with `pnpm i18n:locale:artifacts:idempotence`; `pnpm i18n:corrections:check`; focused locale/runtime tests; `pnpm lint`; `pnpm build` | `pnpm i18n:locale:all:production:check` for a release candidate; `pnpm prepush:gate`; route-view browser smoke for selector, persistence, content-language read/write, framework copy, declared service/reference fallbacks, route/static views under their existing access context, and long-text layout | the one-shot generator processes every locale in explicit `context -> structuralValidation -> quality -> activation` dependency order and emits canonical JSON; every registry locale must match the canonical topology/key/ICU and independently declared UI/content/service/reference capability contracts; production readiness must fail closed while any reference-resource or other activation blocker remains |
| historical German human-review evidence only | use `i18n:de:pilot`, `i18n:de:review:*`, and `i18n:de:delta:review:*` only to validate the immutable Issue #601/#602/#606 snapshot they own | focused historical checker/renderer tests only when that compatibility implementation changes | these commands are outside active German and full-gate dependency paths; do not regenerate a form for a post-baseline correction, and never commit reviewer data |
| active German runtime or post-baseline correction | run `pnpm i18n:audit`, `pnpm i18n:context:check --locale de-DE`, `pnpm i18n:corrections:check`, `pnpm i18n:locale:quality:check --locale de-DE`, final `pnpm i18n:de:audit`, and focused runtime tests | `pnpm build`; applicable route-view/browser smoke; final `pnpm prepush:gate` through `push:checked` | baseline `c26f306e82ac66f50a56aafe8f89ea96c0b0c67d` pins the accepted 2,737-message German catalog/runtime state; new source messages use the normal source-context closure, while a changed existing German value requires an exact tracked correction dossier and never a private confirmation |
| data workflow fixtures or workflow smoke harnesses | `pnpm docpact:gate`; `pnpm test:data-workflows:unit` | affected live smoke script only when credentials and target environment are part of the task, using `pnpm test:workflows --<workflow> <workflow-args>`; `pnpm test:api:smoke <workflow-args>` for broad supported API smoke coverage, then inspect its summary because child workflow failures do not make the command exit non-zero | keep `fixtures/data/**`, `fixtures/result/**`, workflow defaults, and unit path assertions aligned |
| repo docs only | `scripts/docpact lint --root . --files "<csv>" --mode enforce` | `scripts/docpact validate-config --root . --strict` when `.docpact/config.yaml` changes | still update review metadata and ownership as needed |

## Semantic Localization E2E Contract

The canonical command is `pnpm test:e2e:i18n`, implemented with `@playwright/test` `1.61.1`, `playwright.config.ts`, and `tests/e2e/i18n/**`. The candidate frontend is always local (`pnpm start:main`, loopback `E2E_BASE_URL`) while its configured backend is production; pointing the Playwright base URL at a production frontend fails closed.

The independent `.github/workflows/i18n-semantic-e2e.yml` workflow is a credential-free, manually dispatched hermetic qualification boundary:

- `workflow_dispatch` provides the only on-demand trigger; routine pull requests, `dev` pushes, and release aggregation do not call this browser qualification
- every invocation uses no production credentials, permits no production writes, and runs the complete hermetic semantic qualification
- select an open business PR branch or exact SHA before `release:to-dev` when its change risk warrants browser evidence; a failure is fixed and rerun on that same PR
- it always executes the content-addressed qualification's prebuilt candidate, closed simulator, complete Chromium matrix, and Firefox/WebKit critical scenarios; it is not a required release check and no release stage consumes its proof

The separate full authenticated closure is local-operator-only. It requires explicit user authorization, runtime credentials, the local candidate, `E2E_BACKEND_TARGET=production`, and `E2E_AUTHENTICATED=true`. Before Docker execution, the host controller rejects production-data mode whenever `CI` or `GITHUB_ACTIONS` is set. Only after that local check passes does it override the release image's inherited CI markers to empty inside the container; the container's safety check remains unchanged. Its two production-write guards are `E2E_ALLOW_PRODUCTION_DATA=true` and `E2E_PRODUCTION_WRITE_CONFIRMATION=I_AUTHORIZE_ONE_CODEX_E2E_PRODUCTION_PROCESS`; writing verified external evidence separately opts in with `E2E_WRITE_VERIFIED_EVIDENCE=true`. Semantic E2E GitHub Actions is never a transport for these credentials, flags, or writes.

For a normal release, run browser qualification manually on the still-open business PR when the change risk warrants it. The workflow builds and serves the chosen committed candidate against the fixed `.invalid` profile in `docker/e2e/qualification.env`, intercepts every backend request, covers the complete Chromium route/view matrix and Firefox/WebKit critical scenarios, and never reads deployment `.env` or `origin/main:.env`. Its identity covers behavior-affecting source, public assets, config, shared E2E helpers, Git entry mode/type, runtime/test inputs, and the browser environment contract; deployment-only `.env` and root application version metadata are excluded.

`release:to-dev --apply` changes only version metadata plus bounded Docpact review metadata. Its restricted local push proves deterministic structure, Docpact coverage, and static release contracts; it does not run browsers or change proof files. The resulting exact Release PR into `dev` runs the mandatory non-browser release gate—static contracts and the full Jest gate—then emits an external proof bound to the main baseline, dev base/head/tree, version, PR, workflow run/attempt, and artifact. The separate authenticated local closure continues to verify its real candidate target against tracked `main`.

The full route/view proof has 49 stable assertion IDs. Every ID requires its live route scenario plus any target-declared semantic scenarios; these cover anonymous fail-closed navigation, locale fallback/refresh, modal states, authoring options, responsive layout, persisted multilingual content, and reference refresh where applicable. Locales and authoring languages are derived from the typed registries, Chromium runs the entire route/view matrix, and the selector, team authoring, and process lifecycle critical scenarios run in all three browser engines. Adding a registry locale expands the expected locale sequence and invalidates any older evidence automatically.

Authenticated setup may create only UUID-scoped `codex-e2e` process data. It writes an ignored intent ledger before attempting create. Before any delete, cleanup reads the production row by UUID and verifies the authenticated owner, the UUID at its exact ILCD path, and language/marker pairs at each of the five exact multilingual field paths; marker strings scattered elsewhere do not attest ownership. Only then may it delete the exact-ID row version. Evidence must prove `created=cleaned` and `leaked=0`. Screenshots, trace, video, stored auth state, and credential-bearing artifacts are disabled. When external evidence is supplied, the explicit production-readiness command requires its current route contract, backend target, raw `pnpm-lock.yaml`, `pnpm-workspace.yaml`, runtime assets, semantic tests, and declared route/source files to match; drift fails closed. The project version is independent of the pnpm lock, so no lock projection or version-field normalization is permitted: any lock or install-policy byte drift invalidates evidence.

Qualification reuse is computed, not maintained by editing hash records. `pnpm e2e:qualification:key` produces the content key; `pnpm release:proof:verify --proof <path>` validates an external proof against the current candidate. A matching key permits behavior-equivalent reuse, while an exact commit/tree match is reported separately. There is no tracked compatibility or waiver manifest.

The shared Header wraps Umi `SelectLang` with `reload={false}`. Browser proof must show that locale switching preserves the current URL and document identity, refreshes locale-bound reference labels in the mounted page, and prevents a delayed response for the old locale from overwriting the current locale.

The local `pre-push` hook runs `pnpm docpact:gate` first. Ordinary delivery pushes run the full `pnpm prepush:gate` local test gate last, with static release preflight added for main-semantic destinations. Only the repo-owned deterministic release command may request the `release-candidate` or `immutable-promotion` profile; each validates its exact generated branch, destination, base/head, and allowed paths, then runs Docpact plus static release preflight without browsers or the aggregate suite. A no-update push and a raw deletion-only push skip checkpoint/gates because they deliver no candidate content; a checked push accepts `HEAD` only when it resolves to the current exact branch SHA and rejects every other ineligible ref shape before running gates. For ordinary delivery, commit the final controlled tracked change and let the hook own the authoritative full gate once; do not manually run the same full gate immediately before pushing.

A passing feature-branch or `dev`-relative Docpact gate proves only that configured comparison range. The normal `release:to-dev` command therefore supplies the complete cumulative path set from current `main` through the intended `dev` candidate, records only genuine bounded review evidence, rejects any other untracked path, and runs static preflight on the composed commit before creating the version PR. That exact dev Release PR adds the one non-browser full-suite proof. The immutable promotion, main-target PR, and normal post-merge workflow then require structurally matching unchanged-tree merges and the exact bound proof; missing, expired, ambiguous, unavailable, or mismatched proof fails closed and requires a new dev candidate instead of a late aggregate rerun. Browser E2E remains an earlier manual business-PR-stage decision. If a candidate reports `missing-review`, genuinely review and update every required governed document in a new `dev` PR; do not hide active findings with a baseline, waiver, or narrower comparison base.

The hook uses an already-active exact Node.js 24.19.0 from `PATH`, including GitHub `setup-node`, before consulting NVM. It sources NVM and runs `nvm use 24.19.0` only when the active Node is missing or has another version, then fails closed with an explicit version error if Node 24.19.0 is still unavailable.

Run every Umi-generating `test:ci`, coverage, and `prepush:gate` command serially because separate invocations share `.umi-test`. This forbids concurrent commands, not the two workers owned by one coverage coordinator. The full gate already includes coverage; do not surround it with redundant broad test or coverage runs for the same checkpoint.

Full-gate evidence binds the exact committed `HEAD`, tracked tree, Node/dependency state, and test/lint/build/coverage/Docpact/gate configuration. Ignored review content, GitHub body changes, and read-only checks do not create a new repository checkpoint. Any changed controlled tracked input does, and requires one new final full-gate run.

Use `pnpm push:checked <normal git push arguments>` for the final managed push. Its ordinary hook runs the authoritative gates and returns a private gate-bound payload to the wrapper, but hook completion alone does not write an active receipt. When Git supplies no ref updates because the destination is already current, the hook skips checkpoint collection and both gates; a managed no-op still requires a private nonce-bound no-update acknowledgement, and that acknowledgement can never activate a retry receipt. A successful managed original push leaves no receipt. Only when a normal gate payload exists and the original update push returns non-zero does the wrapper activate an ignored, one-hour, bounded single-push-intent receipt under `.local/prepush-gate/`.

After that uncertain or failed transport, follow the emitted `Next: pnpm push:retry` instruction and run it with no arguments. The repo-owned helper rejects operator-supplied remote, ref, SHA, or option arguments; it derives the exact target from the receipt, verifies the bound remote/refspec, HEAD/tree/branch, clean worktree, Node/pnpm, lockfiles, installed pnpm layout and dependency tree, hook/gate inputs, and resolved Docpact base, then internally retries only the gate-bound commit SHA. Its exact-SHA `--no-verify` call is the only allowed bypass. If the remote already equals the target SHA, the helper clears the receipt and succeeds idempotently without another push or gate run. A successful helper transport also clears it. A failed retry may retain the receipt only while the remote remains at the bound pre-push SHA and the one-hour TTL remains valid; a pre-transport verification outage performs no push and leaves the bounded receipt available until verification recovers or the TTL expires. Expiry, malformed state, controlled-input drift, or any other verified remote state fails closed and invalidates it. A missing or invalidated receipt requires a new managed push and hook-owned gate run. Never invoke `git push --no-verify` or `HUSKY=0` manually. This bounded recovery path is not a reusable same-HEAD gate cache.

For deployment-only workflow changes under `.github/workflows/build.yml` or the manual `.github/workflows/ci.yml` fallback, validate the workflow shape directly with YAML parsing, formatter checks, and shell syntax checks for edited deploy scripts. For EdgeOne CLI dependency changes, also validate the pinned temporary install path locally without calling the live deploy command. Do not run production deploy commands locally or from PR validation unless the task explicitly requires exercising live deploy credentials. The deterministic dev Release PR owns the reusable credential-free non-browser proof. A PR targeting `main` keeps the required `Main Candidate / Release Gate` check but only verifies exact promotion lineage, unchanged trees, unchanged main baseline, and the bound dev proof. The automatic EdgeOne Pages release starts from a canonical `main` push and repeats that proof/identity verification; missing, expired, ambiguous, mismatched, unavailable, direct, squash, or rebase cases fail before publication and require a new dev candidate. Successful proof verification permits creation of the matching `v*` tag from `package.json.version` before publication. Manual `v*` tag pushes and `workflow_dispatch` recovery runs remain supported when the target commit is already on `main` and always run a fresh non-browser full gate. Unchanged-version `main` workflow hotfix pushes skip release when the matching tag already belongs to an older `main` commit, and Electron publication must pre-create exactly one tag-scoped draft before the parallel matrix and finish by verifying the exact 12 expected non-empty assets in that one draft. The manual deploy fallback is guarded to `refs/heads/main` and checks out `github.sha`, and ordinary non-release branch pushes belong to the local pre-push gate.

The full Release Gate runs the complete Jest test inventory exactly once through `pnpm prepush:gate`. For a canonical release, that exact dev Release PR execution is the aggregate owner and all normal later stages reuse only its exact proof; explicit recovery events may run one fresh aggregate. To keep the long coverage process bounded without creating near-continuous GC pressure, `test:coverage` first runs only `prepushGateReceipt.test.ts` without coverage, then runs every remaining suite through exactly two Jest workers with a `512MB` per-worker idle-memory recycle boundary. Both invocations use the repository test runner. The absolute boundary permits worker replacement after genuine heap growth while avoiding thresholds below the normal instrumented Jest worker footprint. In CI agent mode, the console prints only stage starts, failed suite/assertion names, and final summaries; complete Jest stdout/stderr plus JSON results remain under `.local/test-logs/**` and are uploaded on success or failure as a seven-day Release Gate artifact. Jest's coordinator retains the unchanged `src/**/*` collection and global 100% thresholds across replacements. The isolated receipt suite imports no `src/**`, so this process model changes neither tested behavior nor source coverage. Do not add a preceding standalone `pnpm test:ci` or coverage run. The separate early LCIA verification remains a lightweight fail-fast publication check and `prepush:gate` retains the authoritative complete quality bar.

The default Jest sequencer starts `prepushGateReceipt.test.ts`, `localeDeliveryContracts.test.ts`, and `releaseWorkflow.test.ts` before ordinary suites on a cold runner. This is scheduling only: it does not alter discovery, assertions, workers, coverage collection, or the complete inventory. The receipt suite may construct its expensive seed once, but every case must receive an isolated copied repository and bare remote with `origin` rebound to that case; focused proof must detect any cross-case Git or receipt leakage.

Treat dataset-validation work under `src/pages/*/sdkValidation.ts`, `src/pages/Utils/validation/**`, `src/components/ValidationIssueModal/index.tsx`, and localized validator copy under `src/locales/**` as shipped runtime work even when most of the change looks like error-message plumbing.

`CalculationBundlePanel` and `LcaReleaseReadPanel` are framework-heavy read-orchestration wrappers excluded from statement instrumentation, matching the existing wrapper policy. Their shipped behavior is still proved by focused component tests covering persisted refresh, directional LCI/LCIA rendering, legacy and authorization empty states, oversized-result protection, source-Process identity resolution, validation evidence, short-lived signed-link retry, and download actions. The underlying `src/services/lcaReleases/api.ts` transport, integrity, error, and decompression contracts remain inside the 100% coverage gate.

Treat `pnpm i18n:audit` as the deterministic structural proof for locale ownership and runtime message references. Pair it with `pnpm i18n:platform:audit` for registry/manifest/capability closure and `pnpm i18n:hardcoding:audit` for business-layer language-literal ownership, then run the registry-driven context, quality, correction, route-view, and activation checks for every active locale. The hardcoding audit must cover equality/ternary branches, logical-or and nullish defaults such as `locale || 'en-US'` and `locale ?? 'zh-CN'`, and fixed root `lang` metadata embedded in an exported complete HTML document. Runtime defaults and exported root `lang`/`dir` must instead derive from the typed locale registry/runtime policy. These tracked gates prove the configured automated quality and language-capability model without creating a human translation-approval state.

Locale tests normally derive their iteration set from the registry. A fixed supported-language array may remain only when the adjacent test text or comment identifies it as an intentional fail-closed product snapshot gate: adding or removing a product language must then fail until the expected product contract is reviewed. Such a snapshot is validation-only, must not feed business behavior, and is not a substitute for registry-parameterized capability coverage.

Canonical-manifest `--check` reuses the checked-in `source.baseRef` and immutable `source.baseCommit` unless `--base-ref` is explicitly supplied. Advancing an ambient branch such as `origin/dev` without changing audited locale/callsite inputs therefore does not stale a release checkout; `--write` and an explicit `--base-ref` still resolve and record the requested current commit.

Clean-checkout Jest, active German validation, new-locale activation, and the repository full gate must succeed without `.local/**confirmation*`. The old German Pilot/catalog/delta tools may still exercise generated private fixtures when their frozen compatibility code is tested, but they are not current-catalog or release-gate inputs.

For active German, do not reinterpret or delete the inherited Issue #601/#602/#606 evidence. The accepted current boundary is pinned at `c26f306e82ac66f50a56aafe8f89ea96c0b0c67d` in `docs/plans/i18n/corrections.json`. New source messages pass the shared context closure. Any post-baseline change to an existing `zh-CN` or `de-DE` value must add one compact dossier with exact before/after text, evidence, affected closure, tests, and digest. `pnpm i18n:corrections:check` proves that the ledger exactly matches the active catalog; no new human confirmation is requested or inferred.

If a coverage change excludes framework-heavy wrapper files from `collectCoverageFrom`, document why those files are excluded and re-check the affected save, validation, navigation, and highlighting flows with focused tests before relying on `pnpm prepush:gate`.

## Minimum PR Validation Note

Every PR note for this repo must state:

1. exact commands run
2. exact focused suites, if any
3. exact environments checked
4. whether `pnpm prepush:gate` ran
5. whether any required proof lives in another repo
6. for semantic localization E2E, which browser/trust boundary ran and, for an authenticated run, the non-secret `created/cleaned/leaked` counts
